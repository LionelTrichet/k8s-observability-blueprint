apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo-service.name" . }}-dashboard
  labels:
    {{- include "demo-service.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  k8s-observability-blueprint.json: |-
{{ .Files.Get "dashboards/overview.json" | indent 4 }}
