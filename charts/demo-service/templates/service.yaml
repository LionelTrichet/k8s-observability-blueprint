apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-service.name" . }}
  labels:
    {{- include "demo-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "demo-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
